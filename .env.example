# Copy to .env.local and fill in real values. Never commit .env.local or any file with real secrets.

# Required: Supabase project URL and anon (public) key
# From Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Required: Secret for NextAuth session signing. Generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-at-least-32-chars"

# Optional: SMS confirmation to patient (Twilio). Free trial at https://www.twilio.com/try-twilio
# TWILIO_ACCOUNT_SID="ACxxxx"
# TWILIO_AUTH_TOKEN="xxxx"
# TWILIO_PHONE_NUMBER="+15551234567"   # Your Twilio phone number (E.164)

# Optional: Email notifications (e.g. doctor notified when appointment is scheduled)
# Get an API key at https://resend.com/api-keys . Free tier: 100 emails/day.
# RESEND_API_KEY="re_xxxx"
# EMAIL_FROM="D.E.C. Portal <notifications@yourdomain.com>"  # optional; defaults to Resend onboarding address for testing

# Optional: Outlook calendar integration (reception + doctors connect in Settings)
# Register an app in Azure AD: https://entra.microsoft.com → App registrations → New
# Add redirect URI: https://your-domain.com/api/auth/microsoft/callback (and http://localhost:3000/... for dev)
# Create a client secret; add Client ID and Secret below. Set APP_URL to your app root (e.g. https://your-domain.com)
# MICROSOFT_CLIENT_ID=""
# MICROSOFT_CLIENT_SECRET=""
# APP_URL="https://your-domain.com"

# Optional: Seed script (npm run db:seed)
# PORTAL_SEED_PASSWORD="changeme"
# PORTAL_SEED_RECEPTION_EMAIL="reception@decoftexas.com"
# PORTAL_SEED_DOCTOR_EMAIL="doctor@decoftexas.com"
